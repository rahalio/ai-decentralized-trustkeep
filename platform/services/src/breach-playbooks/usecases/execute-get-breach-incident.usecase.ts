/**
 * ExecuteGetBreachIncident - Application use case
 *
 * Generated by Use Case Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: breach-playbooks
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * ExecuteGetBreachIncident Use Case
 *
 * DDD: Application use case for get BreachIncident.
 * Orchestrates the business flow using ports (interfaces).
 */

import type { GetBreachIncidentInput, GetBreachIncidentOutput } from "../dto/breach-incident.dto";
import type { ExecutionContextService, IdGeneratorService } from "@trustkeep/services/_shared/index.js";
import type { BreachIncidentRepository } from "../ports";
import { NotFoundError, ValidationError } from "../errors";

export class ExecuteGetBreachIncident {
  constructor(
    private readonly context: ExecutionContextService,
    private readonly idGenerator: IdGeneratorService,
    private readonly breachIncident: BreachIncidentRepository
  ) {}

  async execute(input: GetBreachIncidentInput): Promise<GetBreachIncidentOutput> {
    // Generate correlation ID for this request (domain-prefixed format: {domain_prefix}_{ulid})
    const correlationId = this.idGenerator.brcId();

    // All field names extracted from OpenAPI spec: none
    // Validate input
    if (!input) {
      throw new ValidationError("Input is required");
    }
    if (!(input as any).incidentId) {
      throw new ValidationError("incidentId is required");
    }
    const incidentId = (input as any).incidentId;

    // Load entity via repository (pass correlation ID for response meta)
    const entity = await this.breachIncident.getBreachIncident({ incidentId: incidentId, orgId: this.context.getOrgId(), correlationId } as any);
    if (!entity) {
      throw new NotFoundError(`Entity not found: ${incidentId}`);
    }

    return entity as GetBreachIncidentOutput;
  }
}
