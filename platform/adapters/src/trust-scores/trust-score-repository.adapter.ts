/**
 * TrustScoreRepositoryAdapter - Port Adapter Implementation
 *
 * Generated by Port Adapter Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: trust-scores
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * TrustScoreRepositoryAdapter
 *
 * DDD: Infrastructure adapter that implements TrustScoreRepository port.
 * Handles infrastructure concerns (HTTP, database, external APIs).
 * Encapsulates Ddb creation - api-server passes dynamoClient only.
 */

import type {
  TrustScoreRepository,
} from "@trustkeep/services/trust-scores";
import type { AdapterDynamoDBClient } from "../_shared/dynamodb-client-types.js";
import { TrustScoreRepositoryDdb } from "./trust-score-repository.ddb.js";

/**
 * TrustScoreRepositoryAdapter
 *
 * Implements TrustScoreRepository port for infrastructure layer.
 */
export class TrustScoreRepositoryAdapter implements TrustScoreRepository {
  private readonly ddb: TrustScoreRepositoryDdb;

  constructor(
    private readonly dynamoClient: AdapterDynamoDBClient
  ) {
    this.ddb = new TrustScoreRepositoryDdb(this.dynamoClient);
  }

  async listTrustScores(input: Parameters<TrustScoreRepository['listTrustScores']>[0]): Promise<Awaited<ReturnType<TrustScoreRepository['listTrustScores']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.listTrustScores(input);
  }

  async getTrustScore(input: Parameters<TrustScoreRepository['getTrustScore']>[0]): Promise<Awaited<ReturnType<TrustScoreRepository['getTrustScore']>>> {
    // Delegate to DynamoDB repository
    return await this.ddb.getTrustScore(input);
  }
}
