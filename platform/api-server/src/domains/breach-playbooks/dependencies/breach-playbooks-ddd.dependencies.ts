/**
 * BreachPlaybooks DDD Dependencies - Composition root
 *
 * Generated by Dependencies Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: breach-playbooks
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */

/**
 * BreachPlaybooks Domain Module - Composition root
 *
 * DDD: Composition root for breach-playbooks domain. Exposes repos (ports) and use cases
 * grouped by aggregate. No orgId at build time — use execution context at call time.
 */

import {
  BreachIncidentRepositoryAdapter,
  PlaybookRunRepositoryAdapter,
  TrustDeltumRepositoryAdapter,
} from "@trustkeep/adapters/breach-playbooks";
import { getIdGeneratorService } from "@trustkeep/adapters";
import type { AdapterDynamoDBClient } from "@trustkeep/adapters";
import { executionContextService } from "../../../lib/execution-context.service.js";
// Use-case constructors (concrete)
import {
  ExecuteGetBreachIncident,
  ExecuteListBreachIncidents,
  ExecuteListPlaybookRuns,
  ExecuteOpenBreachIncident,
  ExecuteRecordPlaybookTrustDelta,
  ExecuteStartPlaybookRun,
} from "@trustkeep/services/breach-playbooks/usecases";
// Ports (interfaces)
import type {
  BreachIncidentRepository,
  PlaybookRunRepository,
  TrustDeltumRepository,
} from "@trustkeep/services/breach-playbooks/ports";

export interface BreachPlaybooksDomainModule {
  repos: {
    breachIncidents: BreachIncidentRepository;
    playbookRuns: PlaybookRunRepository;
    trustDelta: TrustDeltumRepository;
  };
  useCases: {
    breachIncidents: {
      get: ExecuteGetBreachIncident;
      list: ExecuteListBreachIncidents;
    };
    playbookRuns: {
      create: ExecuteStartPlaybookRun;
      list: ExecuteListPlaybookRuns;
    };
    trustDelta: {
      get: ExecuteRecordPlaybookTrustDelta;
    };
  };
}

export function buildBreachPlaybooksDomainModule(
  dynamoClient: AdapterDynamoDBClient,
): BreachPlaybooksDomainModule {
  // Adapters implementing ports (adapters encapsulate Ddb)
  const repos = (() => {
    const obj: {
      breachIncidents?: BreachIncidentRepository;
      playbookRuns?: PlaybookRunRepository;
      trustDelta?: TrustDeltumRepository;
    } = {};

    obj.breachIncidents = new BreachIncidentRepositoryAdapter(dynamoClient);
    obj.playbookRuns = new PlaybookRunRepositoryAdapter(dynamoClient);
    obj.trustDelta = new TrustDeltumRepositoryAdapter(dynamoClient);
    return {
      breachIncidents: obj.breachIncidents!,
      playbookRuns: obj.playbookRuns!,
      trustDelta: obj.trustDelta!,
    };
  })();

  const executionContext = executionContextService;
  const idGenerator = getIdGeneratorService();

  // Use cases depend only on ports (repos.*)
  const useCases = {
    breachIncidents: {
      get: new ExecuteGetBreachIncident(executionContext, idGenerator, repos.breachIncidents),
      list: new ExecuteListBreachIncidents(executionContext, idGenerator, repos.breachIncidents),
    },
    playbookRuns: {
      create: new ExecuteStartPlaybookRun(executionContext, idGenerator, repos.playbookRuns),
      list: new ExecuteListPlaybookRuns(executionContext, idGenerator, repos.playbookRuns),
    },
    trustDelta: {
      get: new ExecuteRecordPlaybookTrustDelta(executionContext, idGenerator, repos.trustDelta),
    },
  };
  return { repos, useCases: useCases as BreachPlaybooksDomainModule["useCases"] };
}
