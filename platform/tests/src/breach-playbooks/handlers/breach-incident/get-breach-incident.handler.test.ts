/**
 * GetBreachIncident handler tests
 *
 * Generated by Test Generator v2.0.0
 * Generator Version: 2.0.0
 * Domain: breach-playbooks
 *
 * ⚠️  DO NOT EDIT THIS FILE MANUALLY
 * This file is auto-generated. Any manual changes will be overwritten.
 */


import { describe, it, expect, beforeEach, vi } from "vitest";
import { getBreachIncident } from "@trustkeep/api-server/domains/breach-playbooks/handlers";
import type { BreachPlaybooksDDDDependencies } from "@trustkeep/api-server/domains/breach-playbooks";
import { createBreachIncident as createBreachIncidentEntity } from "@trustkeep/factories/breach-playbooks";
import { createMockRequest, createMockReply, createMockDependencies, TEST_ORG_ID } from "@trustkeep/tests/shared";
import { BREACHPLAYBOOKS_USE_CASES } from "../__factories__/use-cases.js";

describe("GetBreachIncident", () => {
  let mockRequest: ReturnType<typeof createMockRequest>;
  let mockReply: ReturnType<typeof createMockReply>;
  let mockDeps: BreachPlaybooksDDDDependencies;
  const orgId = TEST_ORG_ID;

  beforeEach(() => {
    mockRequest = createMockRequest({ orgId });
    mockReply = createMockReply();
    mockDeps = createMockDependencies<BreachPlaybooksDDDDependencies>(
      BREACHPLAYBOOKS_USE_CASES,
      {
        // Mock use case dependencies here
      }
    );
  });

  it("should return 200 with entity when found", async () => {
    const entity = createBreachIncidentEntity({ orgId });
    const useCaseResult = { data: entity, meta: { correlationId: "test", timestamp: new Date().toISOString() } };
    
    vi.spyOn(mockDeps.executeGetBreachIncident, "execute").mockResolvedValue(useCaseResult);
    mockRequest.params = { incidentId: entity.id };

    await getBreachIncident(mockRequest, mockReply, mockDeps);

    expect(mockDeps.executeGetBreachIncident.execute).toHaveBeenCalledWith(expect.objectContaining({ incidentId: entity.id }));
    expect(mockReply.code).toHaveBeenCalledWith(200);
    expect(mockReply.send).toHaveBeenCalledWith(useCaseResult);
  });

  it("should return error when entity not found", async () => {
    const nonExistentId = "non-existent-id";
    mockRequest.params = { incidentId: nonExistentId };
    
    vi.spyOn(mockDeps.executeGetBreachIncident, "execute").mockRejectedValue(
      new Error("Entity not found")
    );

    await expect(
      getBreachIncident(mockRequest, mockReply, mockDeps)
    ).rejects.toThrow();
  });

});
